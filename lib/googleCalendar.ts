// STUB – will be filled in after Vercel build is confirmed working
import type { Worker } from '@/types'
export async function getAvailableSlotsForDate(_workers: Worker[], _date: string): Promise<string[]> {
  return []
}
export async function getWorkersForSlot(_workers: Worker[], _date: string, _time: string): Promise<Worker[]> {
  return []
}
